{
  "name": "root",
  "private": true,
  "scripts": {
    "start:dev": "lerna run start:dev",
    "build": "lerna run --scope api build",
    "build-all": "lerna run build",
    "start": "lerna run start",
    "bootstrap": "lerna bootstrap",
    "postinstall": "npm run bootstrap && npm run build",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "engines": {
    "node": "10.16.3"
  }
}
